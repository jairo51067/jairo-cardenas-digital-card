// src/features/profile/profile.js
import { renderProfile } from './profile.render.js'; // Quitamos "Card" para que coincida

export function initProfile() {
    renderProfile(); // Llamamos a la función con el nombre correcto
}